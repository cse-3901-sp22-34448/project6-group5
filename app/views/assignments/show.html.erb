<% if notice %>
<br><p class="p1" style="color: green"><%= notice %></p>
<% end %>

<%= render @assignment %>
  <% AssignmentsController.instance_variable_get(:'@x').push(@assignment.new_assignment) %>

<div>
  <%= link_to "Back to assignments", assignments_path %>
</div>
