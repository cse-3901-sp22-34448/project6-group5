
    <% if user_signed_in? %>
      <h1>Hello, <%= current_user.name %>!</h1>
      <p>Your membership: <%= current_user.job %></p>

    <% else %>
      <%= controller.redirect_to "/users/sign_in" %>
    <% end %>